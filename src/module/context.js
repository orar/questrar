// @flow
import React from 'react';

//React Context api
const { Provider: RequestProviderContext, Consumer: RequestConsumerContext } = React.createContext({});

export { RequestProviderContext,  RequestConsumerContext };